# ===========================================================================
# PulmoSeg 3D — Dependencias de Producción (Google Cloud Platform)
# ===========================================================================
#
# Diferencias con requirements_local.txt:
#   + google-cloud-storage  → lectura/escritura de DICOM y NIfTI en GCS
#   + google-cloud-secret-manager → acceso a secretos (API Key, DB URL)
#   + asyncpg               → driver PostgreSQL asíncrono para Cloud SQL
#   + SQLAlchemy[asyncio]   → soporte async para Cloud SQL vía asyncpg
#
# torch, torchvision y torchaudio NO se incluyen aquí porque ya vienen
# preinstalados en la imagen base pytorch/pytorch del Dockerfile.
# ===========================================================================

# --- Motor de Inferencia Médica ---
monai==1.3.2

# --- Procesamiento de Imágenes Médicas ---
numpy<2.0
pydicom==2.4.3
SimpleITK==2.3.1
nibabel==5.2.1
scipy>=1.11.0
scikit-image>=0.22.0

# --- Framework Web (API Gateway) ---
fastapi==0.110.0
hypercorn>=0.16.0
uvloop>=0.19.0
python-multipart==0.0.9

# --- Persistencia (Cloud SQL — PostgreSQL) ---
# psycopg2-binary: driver sincrónico para SQLAlchemy create_engine() con PostgreSQL
# asyncpg: driver asíncrono (reservado para migración futura a create_async_engine)
sqlalchemy==2.0.29
psycopg2-binary>=2.9.9
asyncpg>=0.29.0

# --- Google Cloud SDKs ---
google-cloud-storage>=2.16.0
google-cloud-secret-manager>=2.20.0

# --- Variables de Entorno ---
python-dotenv>=1.0.0

# --- Validación de datos ---
pydantic==2.6.4

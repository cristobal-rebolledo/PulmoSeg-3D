# ===========================================================================
# PulmoSeg 3D — Dependencias para Entorno de Desarrollo Local (Conda)
# ===========================================================================
#
# Regla 4: Este archivo NO incluye torch, torchvision ni torchaudio porque
# ya están preinstalados en el entorno Conda (PyTorch 2.2.0+cu118).
# Incluirlos aquí causaría un downgrade accidental.
#
# Regla 1: Se excluyen todos los SDKs de google-cloud-* porque el entorno
# local usa SQLite, filesystem y BackgroundTasks como sustitutos.
#
# Instalación:
#   conda activate pulmoseg  (o tu entorno con PyTorch 2.2.0)
#   pip install -r requirements_local.txt
# ===========================================================================

# --- Motor de Inferencia Médica ---
# MONAI 1.3.2: última patch estable de la serie 1.3, compatible con PyTorch 2.2.0
monai==1.3.2

# --- Procesamiento de Imágenes Médicas ---
# numpy <2.0: requerido para compatibilidad estable con PyTorch 2.2.0 y MONAI
numpy<2.0
pydicom==2.4.3
SimpleITK==2.3.1
nibabel==5.2.1
scipy>=1.11.0
scikit-image>=0.22.0

# --- Framework Web (API Gateway) ---
fastapi==0.110.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# --- Persistencia Local (Sustituye Firestore) ---
sqlalchemy==2.0.29

# --- Validación de datos (incluido con FastAPI, pero se fija versión) ---
pydantic==2.6.4
